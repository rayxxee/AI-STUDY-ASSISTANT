---
phase: 2
plan: 2
wave: 1
---

# Plan 2.2: LLM Generation & Citation Logic

## Objective
Implement the generation layer using Gemini's text generation API to formulate answers based strictly on the retrieved context, and mandate source citations.

## Context
- .gsd/SPEC.md
- rag/retriever.py

## Tasks

<task type="auto">
  <name>Gemini Answer Generator</name>
  <files>rag/generator.py</files>
  <action>
    - Create `rag/generator.py` containing `generate_answer(query, retrieved_contexts)`.
    - Initialize `google.generativeai.GenerativeModel('gemini-1.5-flash')`.
    - Construct a strict prompt telling the model to answer the query *only* using the provided context, and to explicitly cite the context source. Format the `retrieved_contexts` into a readable string block.
    - Return the generated text string.
  </action>
  <verify>Pass a hardcoded query and context block to `generate_answer` and verify the output uses the context securely without hallucinating.</verify>
  <done>Gemini accurately returns an answer grounded in the context parameter</done>
</task>

## Success Criteria
- [ ] LLM provides answers bounded by the provided context (reducing hallucination).
- [ ] LLM includes basic citations referencing the provided context texts.
