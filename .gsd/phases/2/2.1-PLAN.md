---
phase: 2
plan: 1
wave: 1
---

# Plan 2.1: Retrieval Logic

## Objective
Implement the retrieval component that takes a user query, generates its embedding, and fetches the top-K most relevant document chunks from the FAISS vector store.

## Context
- .gsd/SPEC.md
- rag/vector_store.py
- rag/embeddings.py

## Tasks

<task type="auto">
  <name>Implement Query Embedder & Retriever</name>
  <files>rag/retriever.py</files>
  <action>
    - Create `rag/retriever.py` containing a `retrieve_context(query, vector_store, top_k=3)` function.
    - Inside, call `get_embeddings([query])` (from rag.embeddings) to get the query vector.
    - Pass this vector into `vector_store.search_similar(query_vector, top_k)`.
    - Return the raw retrieved dictionaries containing text and distance.
  </action>
  <verify>Write a quick python script passing a dummy query to `retrieve_context` and verify it returns an array of chunks.</verify>
  <done>Retriever successfully takes a string query and returns semantic text chunks</done>
</task>

## Success Criteria
- [ ] System can successfully isolate top-K context strings from FAISS given an arbitrary query.
