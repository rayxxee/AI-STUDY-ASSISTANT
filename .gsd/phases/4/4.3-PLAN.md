---
phase: 4
plan: 3
wave: 2
---

# Plan 4.3: Summary UI Integration

## Objective
Add a button in the sidebar to generate a 5-bullet summary of the ingested documents by calling the `/summarize` endpoint.

## Context
- .gsd/SPEC.md
- ui/app.py

## Tasks

<task type="auto">
  <name>Global Summarize Action</name>
  <files>ui/app.py</files>
  <action>
    - Add a "Quick Summarize" button `st.button` in the UI sidebar below the uploader.
    - On click, execute `requests.get("http://localhost:5000/summarize")`.
    - Display the summary results nicely in the sidebar using `st.info` or `st.markdown`.
  </action>
  <verify>Click the summarize button while documents exist in the backend and see a formatted 5-point summary.</verify>
  <done>User has one-click access to document summarization capabilities</done>
</task>

## Success Criteria
- [ ] UI clearly exposes the holistic summarization feature on demand.
