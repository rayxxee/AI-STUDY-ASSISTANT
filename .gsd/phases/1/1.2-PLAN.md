---
phase: 1
plan: 2
wave: 1
---

# Plan 1.2: Text Extraction & Chunking

## Objective
Process uploaded documents by extracting their raw text and semantically splitting them into token chunks suitable for embeddings.

## Context
- .gsd/SPEC.md
- .gsd/ARCHITECTURE.md
- routes/upload.py

## Tasks

<task type="auto">
  <name>Text Extraction Utility</name>
  <files>utils/text_processing.py, requirements.txt</files>
  <action>
    - Add PyMuPDF (fitz) or pdfplumber to requirements.
    - Implement `extract_text(filepath, file_type)`.
    - Provide handler paths for `.pdf`, `.docx`, and `.txt` extensions.
    - Return clean, normalized unicode string outputs without excessive malformed whitespace spaces.
  </action>
  <verify>Python script extracting text from a dummy PDF file ensures return is a string</verify>
  <done>Text correctly extracted from all 3 supported formats</done>
</task>

<task type="auto">
  <name>Text Chunking Logic</name>
  <files>utils/text_processing.py</files>
  <action>
    - Implement a chunking method `chunk_text(text, chunk_size=400, overlap=50)`.
    - Ideally use a semantic character splitting approach so paragraphs aren't cut mid-sentence.
    - Output an array of text strings representing the sequential document.
  </action>
  <verify>Pass a 1000-word string to `chunk_text` and ensure block lengths respect constraints</verify>
  <done>Text successfully splits into overlapping chunks with appropriate overlap</done>
</task>

## Success Criteria
- [ ] PyMuPDF extracts readable text from standard notes documents.
- [ ] Texts are safely split into chunks bounded within standard model token limits.
