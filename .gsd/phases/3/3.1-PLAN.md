---
phase: 3
plan: 1
wave: 1
---

# Plan 3.1: Chat Memory System

## Objective
Implement a short-term memory buffer to maintain conversational context across multiple queries, allowing follow-ups.

## Context
- .gsd/SPEC.md
- routes/query.py

## Tasks

<task type="auto">
  <name>Chat Memory Module</name>
  <files>memory/chat_memory.py</files>
  <action>
    - Create `memory/chat_memory.py`.
    - Implement a simple `ChatMemory` class with `add_message(role, content)` and `get_history(limit=5)` methods.
    - Instantiate a continuous global `memory_store` object (simplified for single-user MVP).
  </action>
  <verify>Instantiate `ChatMemory`, add 6 messages, and ensure `get_history(5)` returns only the last 5 elements to test truncation.</verify>
  <done>Chat memory correctly truncates and stores message history</done>
</task>

<task type="auto">
  <name>Integrate Memory into Query Endpoint</name>
  <files>routes/query.py, rag/generator.py</files>
  <action>
    - Update `routes/query.py` to push user queries and LLM generated answers into the imported `ChatMemory`.
    - Pass the chat history string to `generate_answer()` in `rag/generator.py`.
    - Update the Gemini text prompt to gracefully include a "Previous Conversation History" block.
  </action>
  <verify>Send a query 'What is X?', then send a follow-up 'Explain it more' and ensure the LLM answers based on X smoothly without retrieving generic junk.</verify>
  <done>LLM contextually remembers the previous turn's query and response during generation</done>
</task>

## Success Criteria
- [ ] Users can ask accurate follow-up questions explicitly relying on conversational history.
