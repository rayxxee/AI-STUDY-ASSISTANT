---
phase: 3
plan: 2
wave: 1
---

# Plan 3.2: Query Rewriting & Multi-Doc Logic

## Objective
Improve FAISS retrieval accuracy by rewriting vague follow-up user queries into standalone declarative queries using the short-term chat history.

## Context
- .gsd/SPEC.md
- memory/chat_memory.py
- routes/query.py

## Tasks

<task type="auto">
  <name>Implement Query Rewriter</name>
  <files>rag/rewriter.py</files>
  <action>
    - Create `rag/rewriter.py` with `rewrite_query(current_query, chat_history)`.
    - Use the Gemini `GenerativeModel` to construct a standalone search query. 
    - Instruct Gemini: "If the query is already standalone or clear, return it identically as-is."
  </action>
  <verify>Mock a history where the user asked about 'Python' and the current query is 'What are its data types?'. The rewriter should output 'What are Python data types?'.</verify>
  <done>Vague queries are accurately rewritten with contextual subjects extracted from history</done>
</task>

<task type="auto">
  <name>Integrate Rewriter into Pipeline</name>
  <files>routes/query.py</files>
  <action>
    - In `query_endpoint`, call `rewrite_query` BEFORE initializing the FAISS semantic retrieval phase.
    - Pass the *rewritten* query to FAISS.
    - Return the rewritten query in the JSON response payload for observability.
  </action>
  <verify>Verify through the JSON endpoint return that a vague query like 'tell me more' gets rewritten locally before FAISS lookup.</verify>
  <done>Rewritten queries are successfully passed directly to the vector store</done>
</task>

## Success Criteria
- [ ] System actively resolves pronoun ambiguity in follow-up queries ensuring accurate FAISS retrieval.
