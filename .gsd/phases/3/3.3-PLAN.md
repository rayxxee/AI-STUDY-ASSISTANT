---
phase: 3
plan: 3
wave: 2
---

# Plan 3.3: Smart Summarization Endpoint

## Objective
Provide a dedicated endpoint to generate holistic summaries of the knowledge base.

## Context
- .gsd/SPEC.md
- app.py

## Tasks

<task type="auto">
  <name>Summarization Endpoint</name>
  <files>routes/summary.py, app.py</files>
  <action>
    - Create `routes/summary.py` serving a `GET /summarize` route.
    - Implement logic that either dumps the `texts` array from the `vector_store` (with a token limit truncation to avoid API limits) and asks Gemini to provide a crisp 5-bullet-point summary.
    - Register blueprint in `app.py`.
  </action>
  <verify>curl http://localhost:5000/summarize returns a bulleted JSON text summary</verify>
  <done>System generates high-level summaries of ingested content on demand</done>
</task>

## Success Criteria
- [ ] `/summarize` endpoint returns a clear, concise summary of the active document knowledge base.
