---
phase: 5
plan: 1
wave: 1
---

# Plan 5.1: API Integration Tests

## Objective
Establish a test suite for the Flask backend to guarantee endpoint stability (upload, query, summarize) before containerizing.

## Context
- .gsd/SPEC.md
- app.py
- routes/query.py

## Tasks

<task type="auto">
  <name>Setup Pytest and Write Basic Tests</name>
  <files>tests/test_api.py, requirements-dev.txt</files>
  <action>
    - Create `requirements-dev.txt` adding `pytest`.
    - Create a `tests/test_api.py` file.
    - Write a basic Flask test client fixture using `app.py`.
    - Write a test verifying that the `/health` endpoint returns a 200 JSON payload.
    - Write a test for `/query` ensuring it returns 400 when missing the 'question' parameter in the JSON body.
  </action>
  <verify>Run `pip install -r requirements-dev.txt` then `pytest tests/test_api.py`</verify>
  <done>Pytest suite executes locally passing all baseline API assertions.</done>
</task>

## Success Criteria
- [ ] Backend routing logic is protected by basic automated assertions.
